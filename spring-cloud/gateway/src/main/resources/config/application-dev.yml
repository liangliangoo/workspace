spring:
  #  gateway 相关配置
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true  # 是否启动自动识别nacos 服务

    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        username: nacos
        password: nacos

